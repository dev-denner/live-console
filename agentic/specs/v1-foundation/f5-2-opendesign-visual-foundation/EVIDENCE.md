# Evidências — F5.2 OpenDesign visual foundation

## Escopo entregue

Registrar aqui o pacote OpenDesign local, a ligação dos tokens ao frontend e a revisão humana das superfícies V1. Esta fase não instala o aplicativo OpenDesign e não envia dados locais para fora do WSL.

## Validação automatizada

| Comando | Resultado |
| --- | --- |
| `npm test` | preencher após implementação |
| `npm run typecheck` | preencher após implementação |
| `npm run lint` | preencher após implementação |
| `npm run build` | preencher após implementação |
| `git diff --check` | preencher após implementação |

## Validação browser

Registrar capturas ou observações para `/v1/catalogo`, `/v1/lives`, `/v1/blocos`, `/v1/execucao` e `/legacy`. Conferir foco, Escape, viewport, títulos completos e rótulos de mídia no idioma do produto.

## Revisão humana

OpenDesign é uma ferramenta de proposta visual. O contrato deste pacote não substitui decisão de produto, critérios de aceite, revisão humana ou a autoridade do Live Console.
