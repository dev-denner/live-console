{
  "name": "live-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "db:migrate": "node src/migrate.mjs",
    "test": "node --test",
    "typecheck": "node --check server.mjs && node --check src/db.mjs",
    "check": "node --check server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
